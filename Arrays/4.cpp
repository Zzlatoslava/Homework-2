/*#include <iostream> 
#include <vector>

int main4() {
    int N;
    std::cin >> N;
    std::vector <int> A(N); 
    for (int i = 0; i < N; i++) {
        std::cin >> A[i]; 
    }
        for (int p = 1; p < N + 3; p++) {
            std::cout << A[p - 1] << " " << A[p + N - 2] << " ";
            N -= 2;
        }
    
    }*/

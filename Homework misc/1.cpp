/*Задача 1. Написать программу, выводящую все значения беззнаковой переменной типа char в консоль. Запрещено использовать 
что-либо, кроме самой переменной, одного цикла и вывода в поток в теле цикла.*/
#include <iostream>
#include <windows.h>
#include <cmath>
int localize() {
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    int task8();
    return 0;
}